# Traefik Environment Configuration
# Copy this file to .env and customize the values
# IMPORTANT: Never commit the .env file to version control!

# Traefik Dashboard Basic Authentication
# Generate with: echo $(htpasswd -nB user) | sed -e s/\\$/\\$\\$/g
# Or use: docker run --rm httpd:2.4-alpine htpasswd -nbB admin "your_password" | sed -e s/\\$/\\$\\$/g
TRAEFIK_DASHBOARD_CREDENTIALS=admin:$$2y$$10$$your_bcrypt_hash_here

# Cloudflare API Configuration
# The API token file (cf_api_token.txt) should contain your Cloudflare DNS API token
# Get this from Cloudflare Dashboard > My Profile > API Tokens
# Required permissions: Zone:Zone:Read, Zone:DNS:Edit for all zones
# CF_DNS_API_TOKEN=your_cloudflare_dns_api_token_here

# ACME Configuration
# Email address for Let's Encrypt certificate registration and notifications
ACME_EMAIL=youremail@email.com

# ACME server URL - use staging for testing, production for live certificates
# Production: https://acme-v02.api.letsencrypt.org/directory
# Staging: https://acme-staging-v02.api.letsencrypt.org/directory
ACME_CA_SERVER=https://acme-staging-v02.api.letsencrypt.org/directory

# Domain Configuration
# Main domain for your internal services (used for wildcard certificates)
DOMAIN_MAIN=local.example.com

# Wildcard domain pattern for subdomains
DOMAIN_WILDCARD=*.local.example.com

# Traefik dashboard hostname (subdomain of your main domain)
DOMAIN_DASHBOARD=traefik-dashboard.local.example.com

# Optional: External domain configuration
# DOMAIN_EXTERNAL=labratech.org

# Optional: Timezone
# TZ=America/New_York